import 'package:flutter/material.dart';

class UpdateNote extends StatelessWidget {
  const UpdateNote({super.key});


  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(title: Text("UpdateNote"),),
    );
  }
  
}